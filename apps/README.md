# Apps

此目錄放產品程式碼。先從 macOS 原生 App 開始；其他展示工具或測試用 Web UI 之後再另開子目錄。

```text
apps/
├── backend/
└── macos/
    └── MeetingAssistantPrototype/
```

- `backend/`：VPS/local 都能跑的 self-host 逐字稿服務。
- `macos/`：原生 SwiftUI macOS App。
